# Dev-only dependencies for the evals/ package (never installed in the
# production Docker image — see Dockerfile / requirements.txt). Install
# with: pip install -r requirements-eval.txt
#
# `evals/` is intentionally decoupled from the app: no langchain, no
# shared providers with app/ai/langgraph_flow/.
deepeval>=1.0.0
mlflow>=2.16.0                 # full package (local `mlflow ui`); prod uses mlflow-skinny instead, see requirements.txt
pytest>=8.0.0
# openai is not listed explicitly — deepeval already pulls it in
# transitively, and evals/judge.py's OpenRouterJudgeLLM reuses it
# (OpenRouter is OpenAI-API-compatible) rather than adding a redundant SDK.
