Flask>=3.0.0
Flask-SocketIO>=5.3.0
Flask-Login>=0.6.3
Flask-WTF>=1.2.0
Flask-Limiter>=3.8.0
Flask-SQLAlchemy>=3.1.1
Flask-Migrate>=4.0.0
python-dotenv>=1.0.0
qrcode[pil]>=7.4.0
Pillow>=10.0.0
gevent>=24.2.1
gevent-websocket>=0.10.1
gunicorn>=22.0.0
psycopg2-binary>=2.9.9
Werkzeug>=3.0.0
email-validator>=2.0.0

# Part 1.2 — PDF → question set pipeline
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-groq>=0.2.0
langchain-openai>=0.2.0       # NVIDIA NIM fallback tier — OpenAI-compatible endpoint, see llm_utils.py
pdfplumber>=0.11.0
pytesseract>=0.3.10           # wraps the `tesseract` system binary — see README/Dockerfile
tavily-python>=0.5.0          # optional enrich_context node — see nodes/enrich.py; needs TAVILY_API_KEY

# evals/ — client-only, so app/ai/routes.py's best-effort metric-logging
# hook can import it in production. The full `mlflow` package (server UI,
# LLM judge, deepeval) is dev-only, see requirements-eval.txt. No-ops
# entirely unless MLFLOW_TRACKING_URI is set — see evals/config.py.
mlflow-skinny>=2.16.0

# BYOK — encrypts user-saved provider API keys at rest (app/crypto_utils.py).
# Already resolves transitively via google-auth/mlflow/pdfminer, but app
# code now depends on it directly too — pinned so it doesn't silently
# disappear if one of those transitive chains changes.
cryptography>=42.0.0

# Dev tools
ruff>=0.6.0
pytest>=8.0.0                 # tests/test_extract_shapes.py, tests/test_chunk_segments.py —
                               # pure geometry/string unit tests, no DB/Flask context needed
